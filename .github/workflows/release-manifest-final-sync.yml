name: Release Manifest Final Sync

on:
  push:
    branches: [release/production-grade-20260912]
    paths: [.github/workflows/release-manifest-final-sync.yml]

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout release branch
        uses: actions/checkout@v4
        with:
          ref: release/production-grade-20260912

      - name: Remove one-shot workflow and regenerate manifest
        run: |
          rm .github/workflows/release-manifest-final-sync.yml
          git config user.name "placeai-release-bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git ls-files -z | grep -zv '^MANIFEST.sha256$' | sort -z | xargs -0 sha256sum > MANIFEST.sha256
          git add MANIFEST.sha256
          git commit -m "chore: finalize production integrity manifest"
          git push origin HEAD:release/production-grade-20260912
